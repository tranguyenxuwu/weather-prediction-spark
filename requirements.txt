# WeatherPredict — Python Dependencies
# Install: conda activate pyspark && pip install -r requirements.txt

# ── Core ML Pipeline ──
pyspark>=4.0.0
lightgbm>=4.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
numpy>=1.24.0
pandas>=2.0.0
statsmodels>=0.14.0

# ── Streamlit Dashboard ──
streamlit>=1.30.0
duckdb>=0.9.0
pydeck>=0.8.0
folium>=0.14.0
streamlit-folium>=0.15.0
matplotlib>=3.7.0
plotly>=5.15.0

# ── Geospatial (landfall grid) ──
shapely>=2.0.0

# ── Data Preprocessing (run once) ──
xarray>=2023.1.0
netCDF4>=1.6.0
